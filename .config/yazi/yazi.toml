[opener]
   open = [
      { run = 'sxiv -f "$@"', desc = "Open" },
]

   edit = [
      { run = 'nvim "$@"', block = true },      
]

   pdf = [
      { run = 'mupdf "$@"', desc = "Open" },
] 

[open]
prepend_rules = [
   { name = "*.pdf", use = [ "pdf" ] },
]
